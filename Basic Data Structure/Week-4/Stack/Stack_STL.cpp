#include <bits/stdc++.h>

using namespace std;

int main(){
    stack<int> s;
    
    int n;
    cin >> n;
    s.

    for(int i=0; i<n; i++){
        int num;
        cin >> num;
        s.push(num);
        
        }
    while(!s.empty()){
        cout << s.top() << " ";
        s.pop();
    }






    return 0;
}
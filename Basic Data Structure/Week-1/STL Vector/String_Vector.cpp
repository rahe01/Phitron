#include <bits/stdc++.h>

using namespace std;

int main(){
    
    int n;
    cout << "Enter the number of elements: ";
    cin >> n;

    vector<string> v(n);

    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
        /* code */
    }

    for(string s : v){
        cout << s << endl;
    }
    





    return 0;
}
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);

    for (int i = 1; i < n; i++)
    {
        if (i % 21 == 0)
        {
            printf("%d\n", i);
            /* code */
        }
        
        /* code */
    }
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}

#include <stdio.h>

int main()
{

    int a, b;

    scanf("%d %d", &a, &b);
    int sum = a + b;
    printf("inputed number is a = %d b= %d \n", a, b);
    printf("The sum of number is = %d", sum);

    return 0;
}
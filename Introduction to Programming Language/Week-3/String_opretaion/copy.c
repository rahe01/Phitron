#include <stdio.h>
#include <string.h>

int main(){
    char a[10];
    char b[10] = "abcdef";

    int length = strlen();

    for (int i = 0; b[i] != '\0'; i++)
    {
        a[i] = b[i];
        /* code */
    }
    

    printf("%s %s\n", a, b);



    return 0;
}